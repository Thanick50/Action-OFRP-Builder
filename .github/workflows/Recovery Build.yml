name: Recovery Build

on:
  workflow_dispatch:
    inputs:
      device_tree_url:
        description: "Select device tree repository"
        required: true
        type: choice
        default: https://github.com/Thanick50/ofox_device_xiaomi_uke
        options:
          - https://github.com/Thanick50/ofox_device_xiaomi_uke
          - https://github.com/AzzyC/ofox_device_xiaomi_peridot
          - https://gitlab.com/OrangeFox/device/peridot_new

      device_tree_branch:
        description: "Select device tree branch"
        required: true
        default: fox_14.1
        type: choice
        options:
          - fox_14.1
          - auto-dfe

      aosp_mirror:
        description: "Select AOSP Mirror (to bypass Google HTTP 429 Rate Limits)"
        required: true
        type: choice
        default: tuna
        options:
          - tuna
          - ustc
          - none

jobs:
  build:
    if: github.event.repository.owner.id == github.event.sender.id
    runs-on: [self-hosted, linux]
    timeout-minutes: 360
    permissions:
      contents: write
    container:
      image: ubuntu:24.04
      options: --privileged

    defaults:
      run:
        shell: bash

    env:
      SYNC_URL: https://gitlab.com/OrangeFox/sync.git
      MANIFEST_BRANCH: 14.1
      DEVICE_TREE_URL: ${{ inputs.device_tree_url }}
      DEVICE_TREE_BRANCH: ${{ inputs.device_tree_branch }}
      DEVICE_PATH: device/xiaomi/uke
      DEVICE_NAME: uke
      MAKEFILE_NAME: twrp_uke-ap2a-eng
      BUILD_TARGET: recovery

      # Maximise workflow / instance to GitHub's 6 hour limit
      WATCH_UNTIL_SECONDS: 20700
      WATCH_INTERVAL_SECONDS: 600

    steps:

    # Cleanup The Actions Workspace Using Custom Composite Run Actions
    - name: Cleanup
      uses: rokibhasansagar/slimhub_actions@main


    - name: Initialize workspace
      run: |
        rm -rf workspace
        mkdir -p workspace
        cd workspace
        echo "workspace-folder=$(pwd)" >> $GITHUB_OUTPUT
      id: pwd

    - name: Prepare the build environment
      run: |
        apt-get update
        DEBIAN_FRONTEND=noninteractive apt-get install -yq \
            sudo curl git python3 tar aria2
        DEBIAN_FRONTEND=noninteractive apt-get install -yq \
            linux-modules-extra-$(uname -r) || true
        curl -L -O "https://gitlab.com/OrangeFox/misc/scripts/-/raw/master/setup/android_build_env.sh"
        sudo bash android_build_env.sh
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Install OpenJDK
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '11'

    - name: Configure AOSP Mirror
      run: |
        if [ "${{ inputs.aosp_mirror }}" = "tuna" ]; then
          git config --global url."https://mirrors.tuna.tsinghua.edu.cn/git/AOSP/".insteadOf "https://android.googlesource.com/"
          echo "Configured Tsinghua TUNA Mirror for AOSP"
        elif [ "${{ inputs.aosp_mirror }}" = "ustc" ]; then
          git config --global url."https://mirrors.ustc.edu.cn/aosp/".insteadOf "https://android.googlesource.com/"
          echo "Configured USTC Mirror for AOSP"
        else
          echo "Using default Google AOSP servers"
        fi

    - name: Sync OrangeFox sources and minimal manifest
      run: |
        cd ${{ steps.pwd.outputs.workspace-folder }}
        git clone --depth=1 ${{ env.SYNC_URL }}
        cd sync
        ./orangefox_sync.sh --branch ${{ env.MANIFEST_BRANCH }} --path ${{ steps.pwd.outputs.workspace-folder }}/fox_${{ env.MANIFEST_BRANCH }}
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Clone device tree
      run: |
        cd ${{ steps.pwd.outputs.workspace-folder }}/fox_${{ env.MANIFEST_BRANCH }}
        git clone --depth=1 ${{ env.DEVICE_TREE_URL }} -b ${{ env.DEVICE_TREE_BRANCH }} ./${{ env.DEVICE_PATH }}
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Get latest device tree commit
      id: devtree_commit
      run: |
        cd ${{ steps.pwd.outputs.workspace-folder }}/fox_${{ env.MANIFEST_BRANCH }}/${{ env.DEVICE_PATH }}
        LATEST_COMMIT=$(git rev-parse --short=7 HEAD)
        LATEST_COMMIT_FULL=$(git rev-parse HEAD)
        echo "LATEST_COMMIT=$LATEST_COMMIT" >> $GITHUB_OUTPUT
        echo "LATEST_COMMIT_FULL=$LATEST_COMMIT_FULL" >> $GITHUB_OUTPUT
        echo "Latest device tree commit: $LATEST_COMMIT"
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Set Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 30

    - name: Building recovery
      run: |
        cd ${{ steps.pwd.outputs.workspace-folder }}/fox_${{ env.MANIFEST_BRANCH }}

        set +e
        source build/envsetup.sh
        export ALLOW_MISSING_DEPENDENCIES=true
        set -e

        lunch ${{ env.MAKEFILE_NAME }}

        if ! mka adbd ${{ env.BUILD_TARGET }}image -j$(nproc --all); then
          exit 1
        fi
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Check the output directory before uploading
      run: |
        ls ${{ steps.pwd.outputs.workspace-folder }}/fox_${{ env.MANIFEST_BRANCH }}/out/target/product/${{ env.DEVICE_NAME }}/

    - name: Upload to Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          ${{ steps.pwd.outputs.workspace-folder }}/fox_${{ env.MANIFEST_BRANCH }}/out/target/product/${{ env.DEVICE_NAME }}/recovery.img
          ${{ steps.pwd.outputs.workspace-folder }}/fox_${{ env.MANIFEST_BRANCH }}/out/target/product/${{ env.DEVICE_NAME }}/OrangeFox-R*.zip
        name: ${{ env.DEVICE_NAME }}-${{ github.run_id }}
        tag_name: ${{ github.run_id }}
        body: |
          Manifest: ${{ env.MANIFEST_BRANCH }}
          Device: ${{ env.DEVICE_NAME }}
          Target: ${{ env.BUILD_TARGET }}
          Device tree commit: [${{ steps.devtree_commit.outputs.LATEST_COMMIT }}](${{ env.DEVICE_TREE_URL }}/tree/${{ steps.devtree_commit.outputs.LATEST_COMMIT }})
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}



    - name: Watch for device tree changes and dirty build
      if: success()
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        START_TIME=$(date +%s)
        BUILD_COUNT=0
        CURRENT_COMMIT="${{ steps.devtree_commit.outputs.LATEST_COMMIT_FULL }}"

        WORKSPACE="${{ steps.pwd.outputs.workspace-folder }}"
        FOX_DIR="$WORKSPACE/fox_${{ env.MANIFEST_BRANCH }}"
        DEVICE_DIR="$FOX_DIR/${{ env.DEVICE_PATH }}"
        OUT_DIR="$FOX_DIR/out/target/product/${{ env.DEVICE_NAME }}"

        while true; do
          NOW=$(date +%s)
          ELAPSED=$((NOW - START_TIME))

          if [ "$ELAPSED" -ge "${{ env.WATCH_UNTIL_SECONDS }}" ]; then
            break
          fi

          sleep "${{ env.WATCH_INTERVAL_SECONDS }}"

          NOW=$(date +%s)
          ELAPSED=$((NOW - START_TIME))

          if [ "$ELAPSED" -ge "${{ env.WATCH_UNTIL_SECONDS }}" ]; then
            break
          fi

          cd "$DEVICE_DIR"

          git fetch origin "${{ env.DEVICE_TREE_BRANCH }}"

          NEW_COMMIT=$(git rev-parse "origin/${{ env.DEVICE_TREE_BRANCH }}")
          NEW_COMMIT_SHORT=$(echo "$NEW_COMMIT" | cut -c1-7)
          CURRENT_COMMIT_SHORT=$(echo "$CURRENT_COMMIT" | cut -c1-7)

          if [ "$NEW_COMMIT" = "$CURRENT_COMMIT" ]; then
            echo "No device tree change detected. Current commit: $CURRENT_COMMIT_SHORT"
            continue
          fi

          BUILD_COUNT=$((BUILD_COUNT + 1))

          git reset --hard "$NEW_COMMIT"
          CURRENT_COMMIT="$NEW_COMMIT"

          cd "$FOX_DIR"

          set +e
          source build/envsetup.sh
          export ALLOW_MISSING_DEPENDENCIES=true
          set -e

          lunch ${{ env.MAKEFILE_NAME }}

          if ! mka adbd ${{ env.BUILD_TARGET }}image -j$(nproc --all); then
            continue
          fi

          TAG_NAME="${{ github.run_id }}-${BUILD_COUNT}"
          RELEASE_NAME="${{ env.DEVICE_NAME }}-${TAG_NAME}"

          gh release create "$TAG_NAME" \
            "$OUT_DIR"/OrangeFox-R*.img \
            "$OUT_DIR"/OrangeFox-R*.zip \
            "$OUT_DIR"/OrangeFox-R*.md5 \
            --repo "${{ github.repository }}" \
            --title "$RELEASE_NAME" \
            --notes "Manifest: ${{ env.MANIFEST_BRANCH }}
          Device: ${{ env.DEVICE_NAME }}
          Target: ${{ env.BUILD_TARGET }}
          Dirty build: #${BUILD_COUNT}
          Device tree commit: ${NEW_COMMIT_SHORT}
          Device tree URL: ${{ env.DEVICE_TREE_URL }}/tree/${NEW_COMMIT_SHORT}"
        done
